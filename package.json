{
  "name": "ngx-sanitizer",
  "version": "0.0.2",
  "description": "Angular pipes to mark HTML content as trustable.",
  "scripts": {
    "build": "ng build ngx-sanitizer --prod",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@angular/common": ">= 8",
    "@angular/core": ">= 8"
  }
}
